## Copy sql migration files, run PostgreSQL, perform slashing protection schema migration and
## start Web3Signer
`cd pg;docker-compose -f ./docker-compose-pg.yml up`

## Stop container (from another terminal window)
`cd pg;docker-compose -f ./docker-compose-web3signer.yml stop`

## Shutdown container completely and deleting all db volumes (from another terminal window)
`cd pg;docker-compose -f ./docker-compose-pg.yml down -v`

